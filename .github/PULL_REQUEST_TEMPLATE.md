<!-- Certificate submission -->
<!-- Complete all checkboxes and update your certificate URL before submitting -->

### Checklist

<!-- File naming: must be derived from your registered email address -->
- [ ] File is in `data/` and named from my registered email (e.g. `jane-doe-at-example-com.json`)
- [ ] `certificate_id` matches the filename exactly (without `.json`)
- [ ] `name`, `email`, and `description` are my own — not copied from the placeholder values in `data/jane-doe-at-example-com.json`

<!-- Required field values — set by the organiser, copy exactly as instructed -->
- [ ] `workshop` matches the exact workshop title provided by the organiser
- [ ] `date` and `date_iso` match what the organiser specified

---

### My certificate URL (fill in before submitting)

<!-- Replace YOUR-ID-HERE with your actual certificate_id value -->
`https://YOUR_USERNAME.github.io/certificate-generator/?id=YOUR-ID-HERE`
